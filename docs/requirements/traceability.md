# Traceability matrix

Built in D0 per `docs/requirements/plan.md` and enforced by LWP-R12
(`scripts/lwp_check_trace.py`, `tests/test_lwp_check_trace.py`). One row
per `LWP-R` ID heading in `requirements.md` (R1-R39). `LWP-R99` is not a
row here: it appears only inside R12's own **Break** example text as a
fixture heading that should have no row, not a real requirement.

## Status column, and why it exists

Only D0's requirements have a check and tests landed today. R7 and
R13-R38 are proven in D1-D5, by scripts and tests that do not exist in
this tree yet — their **Test:** line in `requirements.md` names the
*future* test node, not a node that resolves right now.

So every row carries a `status`:

- **`proven`** — the named test node(s) actually resolve today: the file
  exists, and (for a `path::function` node) that file contains a matching
  `def function` line. `scripts/lwp_check_trace.py` re-checks this on
  every run; a `proven` row whose test node stops resolving (the test was
  deleted, renamed, or the file moved) fails the build.
- **`planned`** — the row names the test node `requirements.md` promises
  for the phase that will prove it; `scripts/lwp_check_trace.py` does not
  require it to resolve yet. This is the plan, not yet the proof.

A requirement's `Phase:` in `requirements.md` being `D0` does not by
itself make its row `proven` here — the row flips only once its named
test node actually resolves. All twelve D0 requirements (R1-R6, R8-R12,
R39) are `proven` as of this edit; every other row is `planned`. Re-run
`scripts/lwp_check_trace.py` after adding a test to see a row flip.

R1's row is `proven` in the narrow sense the phase allows: the check and
its tests exist and pass, including against this tree with the
pre-recast legacy exemption active. The rule is not yet proven against
real `lwp`-named plugins — that is D2 (Codex) and D4 (Claude).

## Matrix

| ID | Rule summary | Phase | Status | Test node(s) | Break |
|---|---|---|---|---|---|
| LWP-R1 | Every skill dir, entrypoint and manifest/marketplace name starts with/equals `lwp` | D0 (check), D2/D4 (real plugins) | proven | `tests/test_lwp_check_prefix.py::test_valid_tree_passes`, `tests/test_lwp_check_prefix.py::test_break_renamed_skill_dir_and_wrong_manifest_name`, `tests/test_lwp_check_prefix.py::test_scripts_entrypoint_without_prefix_fails` | rename a skill dir to `pulse-status` and set one manifest name to `LEAPWare-Pulse`; the check exits 1 and names both |
| LWP-R2 | Every commit in a PR range is authored `LEAPWare <leapware@outlook.com>` or an allow-listed bot; full-history mode stops at the legacy boundary | D0 | proven | `tests/test_lwp_check_commit_identity.py::test_range_mode_wrong_author_fails`, `tests/test_lwp_check_commit_identity.py::test_full_history_legacy_identity_above_boundary_fails` | a fixture commit with a different author inside the range, and a legacy-identity commit above the boundary; each exits 1 |
| LWP-R3 | No tracked file or added line in a PR range contains an absolute user path, local hostname, or private name | D0 (check), D2 (recast tree) | proven | `tests/test_lwp_check_env_leak.py::test_full_tree_scan_of_fixture_tree_catches_absolute_path`, `tests/test_lwp_check_env_leak.py::test_range_scan_catches_leak_committed_then_reverted` | add a drive-letter user path to a fixture file (exit 1); add it in one commit and revert it in the next, then scan the range (still exit 1) |
| LWP-R4 | No shipped runtime file (`core/`, `adapters/`, `plugins/**`) reads `CLAUDE.md` or `AGENTS.md` | D0 | proven | `tests/test_no_instruction_file_dependency.py::test_scan_finds_no_reference_in_this_checkout`, `tests/test_no_instruction_file_dependency.py::test_scan_catches_a_planted_reference_to_agents_md` | add an `open("AGENTS.md")` line to a fixture core module; exit 1 |
| LWP-R5 | `.github/rulesets/main.json` shape: default branch, no bypass, blocks deletion/non-fast-forward, squash-only PR + merge queue, required checks equal `ci.yml`'s job names | D0 (files), G-PUB (applied) | proven | `tests/test_rulesets_shape.py::test_main_ruleset_has_no_bypass_actors`, `tests/test_rulesets_shape.py::test_main_ruleset_required_status_checks_equal_ci_job_names` | add one bypass actor (fail); rename one CI matrix job without updating the ruleset (fail) |
| LWP-R6 | CI test matrix covers `windows-latest`/`macos-latest`/`ubuntu-latest` x Python 3.10/3.12 | D0 | proven | `tests/test_ci_matrix.py::test_test_job_produces_exactly_six_combinations` | drop `macos-latest` from the matrix; the test fails |
| LWP-R7 | `core/`, `adapters/`, `plugins/*/lwp/` import only the Python 3.10 standard library and own package modules | D1 | planned | `tests/test_stdlib_only.py::test_break_third_party_import_fails` | add `import yaml` to a fixture core module; the test fails |
| LWP-R8 | Codex-lane commits touch only Codex-lane paths, Claude-lane only Claude-lane paths; shared paths need both CLIs' review records | D0 | proven | `tests/test_lwp_lanes.py::test_check_lanes_flags_out_of_lane_commit`, `tests/test_lwp_lanes.py::test_review_ok_missing_review_record_fails`, `tests/test_lwp_check_lane_write.py::test_claude_edit_out_of_lane_denied` | a fixture Codex-lane commit touching `plugins/claude/lwp/` (fail); a shared-path change with only one CLI's review record (fail) |
| LWP-R9 | Every linked worktree is under `<repo>/.worktrees/<branch>`; `.gitignore` has the entry; no sibling folder is a worktree or clone of the same origin; `AGENTS.md`/`CLAUDE.md` state the rule | D0 | proven | `tests/test_lwp_check_worktrees.py::test_sibling_worktree_outside_dot_worktrees_fails`, `tests/test_lwp_check_worktrees.py::test_sibling_clone_of_same_origin_fails`, `tests/test_lwp_check_worktrees.py::test_gitignore_missing_worktrees_entry_fails`, `tests/test_lwp_check_worktrees.py::test_missing_rule_sentence_in_claude_md_fails` | `git worktree add ../sibling-wt` in a fixture (exit 1, names the path); clone the fixture into a sibling folder (exit 1); remove `.worktrees/` from the fixture `.gitignore` (exit 1); delete the rule sentence from the fixture `CLAUDE.md` (exit 1) |
| LWP-R10 | Every `proof/*.json` validates against `proof/schema.json`; `checked_by` differs from `author`; each command's `exit` equals `expect_exit`; each phase has a record before its gate closes | D0 | proven | `tests/test_lwp_check_proof.py::test_self_certified_record_is_rejected`, `tests/test_lwp_check_proof.py::test_exit_mismatch_record_is_rejected`, `tests/test_lwp_check_proof.py::test_real_proof_directory_validates_clean` | set `checked_by` equal to `author` in a fixture record (exit 1); set one `exit` different from `expect_exit` (exit 1) |
| LWP-R11 | `HANDOFF.md` is at most 6000 bytes, carries the begin/end markers, contains no absolute path; `--write` regenerates only the marked block | D0 | proven | `tests/core/test_lwp_handoff.py::test_over_size_cap_fails`, `tests/core/test_lwp_handoff.py::test_absolute_path_fails`, `tests/core/test_lwp_handoff.py::test_cmd_write_regenerates_only_the_block` | pad a fixture to 6001 bytes (fail); insert a drive-letter path (fail); edit prose outside the markers and run `--write` (prose unchanged) |
| LWP-R12 | Every `LWP-R` ID has a traceability row naming an existing test node and a break description; no row names an unknown ID | D0 (check), every phase (rows) | proven | `tests/test_lwp_check_trace.py::test_proven_row_with_deleted_test_function_fails`, `tests/test_lwp_check_trace.py::test_id_with_no_row_fails` | delete a referenced test function (fail); add an `LWP-R99` heading with no row (fail) |
| LWP-R13 | Task reader counts `- [x]`/`- [X]` checked, `- [ ]` unchecked, reports `checked`/`total`, refuses a task doc over 256 KiB | D1 | planned | `tests/core/test_tasks.py::test_break_1mib_cap_fails` | treat `[X]` as unchecked, or raise the cap to 1 MiB; the golden and cap tests fail |
| LWP-R14 | Without both `--spec` and `--evidence`, `verification` is `unknown`; one alone is refused | D1 | planned | `tests/core/test_report.py::test_checked_tasks_are_not_verification` | (ported) supplying only one flag is refused; `pushed`/`packaged`/`active`/`tokens` stay `unknown` |
| LWP-R15 | A passing receipt whose candidate SHA is not HEAD is refused | D1 | planned | `tests/adapters/test_evidence.py::test_real_passing_receipt_from_previous_revision_is_refused` | remove the candidate-equals-HEAD comparison; the stale receipt is accepted and the test fails |
| LWP-R16 | Panorama/Brief/Focus contract has one source of truth, vendored byte-identically into both plugins | D2 (Codex), D4 (Claude) | planned | `tests/test_vendor_committed.py::test_claude_and_codex_vendor_copies_match` | edit one sentence in the Claude copy of `formats.md` only; the drift check fails |
| LWP-R17 | Shipped contract states every listed rule (full plan retained, unknown denominator, floor%/ten-cell bar, unfinished criteria unchecked, optional work outside totals, view-override rules, evidence gate, main-clean rule, status grants no authority) | D2, D4 | planned | `tests/test_lwp_validate_codex_plugin.py::test_each_contract_sentence_required`, `tests/test_lwp_validate_claude_plugin.py::test_each_contract_sentence_required` | for each rule, delete its sentence from a fixture copy of each host's skill; each deletion fails its named case |
| LWP-R18 | Behavioral scenarios (`BHV-*`, `BP-*`) pass on Codex and Claude Code, judged by a reviewer who did not author the skill | D2 (Codex), D4 (Claude) | planned | `tests/behavioral/README.md` protocol (no automated node; judged run) | run `BHV-03` against a skill copy with the unknown-denominator rule removed; the judged result must be FAIL |
| LWP-R19 | Neither plugin registers hooks, MCP servers, apps or background agents; contract states a status request authorizes no fix/install/commit/deploy | D2, D4 | planned | `tests/test_lwp_validate_codex_plugin.py::test_no_mcp_servers_registered`, `tests/test_lwp_validate_claude_plugin.py::test_no_hooks_registered` | add a `hooks` entry to the Claude manifest, or an `mcpServers` entry to the Codex manifest; the validator fails |
| LWP-R20 | A boundary failure capture records only exception class and hashes; no message text or input bytes leave the caller | D1 | planned | `tests/adapters/test_capture.py::test_malformed_input_captured_without_private_payload` | put `str(error)` into the `actual` field; fail |
| LWP-R21 | An unavailable registry yields `health: unavailable` with a warning, never `captured` | D1 | planned | `tests/adapters/test_capture.py::test_registry_failure_visible` | return `captured` from the exception path; fail |
| LWP-R22 | Storage ceilings: 8 KiB/record, 8 MiB/project, 24 MiB aggregate; admission refused at capacity, nothing pruned silently | D1 | planned | `tests/adapters/test_capture.py::test_cap_capture_failure_visible` | raise the record limit to 16 KiB; fail |
| LWP-R23 | `.leapware/local/learning/.gitignore` with exactly `*\n`, created exclusively, before the first incident write | D1 | planned | `tests/adapters/test_capture.py::test_capture_is_ignored_in_fresh_consumer_git_root` | skip marker creation; `git status` in the fixture shows the incident and the test fails |
| LWP-R24 | Lesson lifecycle: stale/wrong-scope/withdrawn inactive; a recurrence invalidates active lessons; mismatched or edited consumer export refused; release pin shape | D1 | planned | `tests/adapters/test_lessons.py::test_stale_wrongscope_and_withdrawn_lesson_not_active` | treat a withdrawn lesson as active; fail. Edit one byte of a fixture export; must still be refused |
| LWP-R25 | Distinct sessions get distinct occurrences; unchanged event is a no-op; contended capture reports unavailable then reconciles without duplicates | D1 | planned | `tests/adapters/test_concurrency.py::test_contended_capture_is_unavailable_then_reconciled_without_duplicates` | bypass the registry lock; duplicates appear and the test fails (20 iterations in the proof run) |
| LWP-R26 | `LEAPWARE_LEARNING_HOME` wins; else `%LOCALAPPDATA%/LEAPWare/state/learning`; else `~/.local/share/LEAPWare/state/learning`; resolution writes nothing | D1 | planned | `tests/adapters/test_registry_path.py::test_break_unconditional_localappdata_fails_on_posix` | read `os.environ["LOCALAPPDATA"]` unconditionally; the macOS and Linux jobs fail |
| LWP-R27 | Learning records keep plugin key `leapware-pulse` and consumer paths, so 0.2.x registries keep working under lwp | D1 | planned | `tests/adapters/test_legacy_registry.py::test_legacy_registry_key_still_resolves` | change the key to `lwp`; retrieval returns no lesson; fail |
| LWP-R28 | Traversal refused before canonicalization; symlinks/junctions refused; Windows 8.3 short alias of the root accepted | D1 | planned | `tests/adapters/test_path_safety.py::test_root_traversal_is_refused_before_canonicalization` | remove the lexical traversal check (fail); remove alias normalization from the acceptance runner (fails with `write escapes managed scratch root`) |
| LWP-R29 | `core/lwp_core` does no I/O: no file, stdin, environment, clock, subprocess or network access | D1 | planned | `tests/core/test_core_is_pure.py::test_break_subprocess_import_fails` | add `import subprocess` to a fixture core module; fail |
| LWP-R30 | Codex plugin manifest name `lwp`, semver version, existing `skills` path, exact file inventory; marketplace points at it | D2 | planned | `tests/test_lwp_validate_codex_plugin.py::test_break_marketplace_path_mismatch_fails` | change the marketplace path to `./plugins/LEAPWare-Pulse`; fail |
| LWP-R31 | Claude plugin manifest and marketplace well formed, source path exists, exact inventory | D4 | planned | `tests/test_lwp_validate_claude_plugin.py::test_break_missing_source_path_fails` | point the marketplace `source` at a missing directory; fail |
| LWP-R32 | Same fixture project through Claude and Codex report entrypoints yields byte-identical report JSON | D4 | planned | `tests/conformance/test_same_report_both_adapters.py::test_break_reordered_keys_fails` | reorder keys in the Codex adapter's output; fail |
| LWP-R33 | Each skill's documented helper command resolves the plugin root and a Python 3 interpreter on every OS without a machine-specific path | D2, D4 | planned | `tests/test_lwp_check_helper_launch.py::test_break_windows_only_launcher_fails_posix` | change the documented command to a Windows-only launcher; the macOS and Linux jobs fail |
| LWP-R34 | `lwp_migrate_codex.py --config <path>` is read-only: detects a pre-recast install, prints remove/add commands, exits 3; no old install exits 0 | D3 | planned | `tests/test_lwp_migrate_codex.py::test_byte_equality_of_config_after_run` | make the script return 0 on an old install (fail); make it write the config (byte-equality assertion fails) |
| LWP-R35 | Both manifests, both marketplace entries and the top `CHANGELOG.md` entry carry the same version; a release tag equals it | D2 (Codex), D4 (both) | planned | `tests/test_lwp_release.py::test_break_codex_manifest_only_bump_fails` | bump the Codex manifest only; fail |
| LWP-R36 | Each shipped plugin tree stays within a byte budget set in D2 from measured recast size + 20% | D2 | planned | `tests/test_lwp_build.py::test_break_1mib_fixture_file_fails_budget` | add a 1 MiB fixture file to a vendor tree; fail |
| LWP-R37 | Scripted rehearsal on each OS covers checked-but-unverified tasks, a stale receipt, a passing source-bound receipt, unavailable capture, lesson retrieval, all three views on both hosts | D5 | planned | `tests/test_lwp_rehearsal.py::test_break_stale_receipt_reported_source_bound_pass_fails` | plant a passing receipt from a previous commit and change the expected output to `source-bound pass`; the job fails because the real run reports `unverified` |
| LWP-R38 | `proof/LWP-D5.json` checked by a different identity from its author, on the other CLI, listing every unproven item explicitly | D5 | planned | `tests/test_lwp_check_proof.py::test_d5_author_and_checker_cli_must_differ` | set both to the same CLI in a fixture; fail |
| LWP-R39 | Every `runs-on` in every workflow, including matrix expansions, is a GitHub-hosted label (`ubuntu-*`/`windows-*`/`macos-*`); never self-hosted, a custom label, or a runner group | D0 | proven | `tests/test_lwp_check_runners.py::test_self_hosted_runs_on_fails_and_names_file_and_job`, `tests/test_lwp_check_runners.py::test_self_hosted_in_matrix_os_list_fails`, `tests/test_lwp_check_runners.py::test_runner_group_mapping_fails` | add `runs-on: self-hosted` to a fixture workflow (exit 1, names the file and job); add `self-hosted` to a matrix `os` list (exit 1); add a `group:` runner mapping (exit 1) |
