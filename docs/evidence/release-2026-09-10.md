# LEAPWare-status private release candidate — 2026-09-10

## Verdict

**LOCAL CI REMEDIATION PREPARED; NOT RELEASED.** The first remote CI run is recorded below as pass-with-warning and therefore RED for the release gate. No clean CI verification, main integration, marketplace change, reinstall, fresh task, or tag is claimed for the final reviewed head.

## Candidate identity

- Repository: private `LEAPWare-Software/LEAPWare-Status`; LEAPWare-only scope.
- Local branch: `work/status-private-release`.
- Evidence baseline: `f17832fdca1bdd5efd6690c56c0abfd9473fe347` (`Record corrected status behavior reruns`).
- Immutable plugin packaging provenance: `977c1f7d19727b4058ed900ede015d7e16a1c21d` (`Prepare private status release candidate`). This identifies the plugin artifacts only; it is not the release head to push or integrate.
- Plugin: `LEAPWare-status`.
- Candidate plugin version: `0.1.0+codex.20260910194515`.
- Manifest change mechanism: `py -3.12 C:\\Users\\leapw\\.codex\\skills\\.system\\plugin-creator\\scripts\\update_plugin_cachebuster.py plugins\\LEAPWare-status`.
- Marketplace/configuration changes: none. The cachebuster helper changed only `plugins/LEAPWare-status/.codex-plugin/plugin.json`.
- Canonical manifest identity: Git blob `c5c626606700432d5f9b9e653576fd9253979dc5` at the candidate packaging commit; SHA-256 of those exact Git-blob bytes is `e8f873c019cc4e36fd15f2c2b1725a7b633a6d7a2b798b9c7d44abf25d941af8`.
- Canonical candidate file SHA-256 values (hash the bytes addressed by the candidate commit, not checkout bytes): `plugin.json` `e8f873c019cc4e36fd15f2c2b1725a7b633a6d7a2b798b9c7d44abf25d941af8`; `SKILL.md` `797e4ad926d0218bc63f5522934d2b317e748cdd853cabcc4932e57cf54fbb4a`; `agents/openai.yaml` `6b79f338be5760fa77e60f14760c14bad2ee99d78c89715789e8e110cc790129`.
- Windows checkout note: before the packaging commit, `plugin.json` checked out with CRLF hashed to `117b375a6c6fdeae4c4626e896d569813325a704ba6e9b6782df415d8e6f65be`. That is a checkout-byte observation only; it does **not** identify the committed candidate and must not be used for private-main or installed-artifact comparison.

The Phase A evidence-fix commits do not modify the plugin tree or version. Use the canonical candidate file hashes above for later private-main and installed-artifact comparison; record separately any platform checkout-byte hash if that diagnostic is needed. The reviewed release head is the final `work/status-private-release` branch tip that descends from `977c1f7` and contains every approved evidence fix; its exact SHA is captured at push time.

## Established evidence

| Evidence class | Established result | Source-bound limit |
| --- | --- | --- |
| Deterministic tests | 12/12 mutation/acceptance tests passed at `f17832f`; Phase A reruns are recorded below. | Tests verify source invariants, not installed behavior. |
| Repository validator | `validate_status_plugin.py` passed at `f17832f`; Phase A rerun is recorded below. | Repository validator is not Codex ingestion. |
| Plugin-creator validator | Passed at `f17832f`; Phase A rerun is recorded below. | Manifest validation does not install the plugin. |
| Independent behavioral QA | Corrected-source `BHV-03`, `BHV-04`, and selected `BHV-01` regression passed 38/38 properties; initial failures remain preserved. | `BHV-02` and `BHV-05`–`BHV-07` were not rerun against corrected source; evaluations were source-path tests, not live discovery. |
| Source review | Task 3 review of `9083181..f17832f` recorded no findings. | Does not cover remote/CI/runtime gates. |

## Remote CI — RED warning evidence

- GitHub Actions run: `34525323554`.
- Exact reviewed head tested: `ddf26ed`.
- Test result: 12/12 tests passed.
- Gate verdict: **RED / not accepted**. GitHub emitted a Node.js 20 deprecation annotation because `.github/workflows/validate.yml` used `actions/checkout@v4` and `actions/setup-python@v5`.
- Local remediation: this reviewed change updates only those action majors to `actions/checkout@v7` and `actions/setup-python@v7`.
- Required next evidence: push the new final reviewed tip, capture its exact remote SHA, and record a clean GitHub Actions run for that SHA. Do not treat run `34525323554` as the final CI gate.

Full provenance, raw outputs, hashes, and limits are in `docs/evidence/behavioral-validation-2026-09-10.md`. The Task 1 deterministic-validator report and Task 3 correction report are retained under `.superpowers/sdd/2026-09-10-status-release/`.

## Phase A local gate results

The commands in this section ran against the uncommitted local candidate after the cachebuster and documentation changes, before the candidate commit.

- `py -3.12 -m unittest discover -s tests -p 'test_*.py' -v` — exit 0; 12 tests ran in 0.345 seconds; all passed.
- `py -3.12 scripts\validate_status_plugin.py plugins\LEAPWare-status` — exit 0; `LEAPWare-status validation passed`.
- `py -3.12 C:\Users\leapw\.codex\skills\.system\plugin-creator\scripts\validate_plugin.py plugins\LEAPWare-status` — exit 0; plugin validation passed.
- `git diff --check` — exit 0; no whitespace errors. Git emitted only its non-failing CRLF-to-LF working-copy warning for `plugin.json`.
- No deterministic test was skipped. The established required independent behavioral reruns were `BHV-03`, `BHV-04`, and selected `BHV-01`; their evidence reports no skip, error, or truncation within that protocol.

## Current blockers and exact planned external checks

1. Confirm the final reviewed `work/status-private-release` branch tip descends from `977c1f7` and includes every approved evidence fix **and** the `actions/checkout@v7` / `actions/setup-python@v7` remediation. Push that reviewed tip without further change and capture its exact remote SHA.
2. Confirm the private GitHub Actions validation workflow is clean for that captured reviewed-head SHA. The earlier run `34525323554` is pass-with-warning RED evidence only.
3. Complete the controller-authorized review/integration of that same captured reviewed-head SHA into private `main`; verify local `main` and `origin/main` identify the same SHA.
4. Reinstall with `codex plugin add LEAPWare-status@LEAPWare-Status`; record installed/enabled identity. Derive the private-main SHA-256 values from exact `git cat-file blob <commit>:<path>` bytes and require the raw installed bytes of `plugin.json`, `SKILL.md`, and `agents/openai.yaml` to match those canonical hashes. If an installed raw hash differs, record it as a mismatch; line-ending normalization may be used only as a diagnostic, never as an unrecorded substitute for canonical identity.
5. In separate fresh Codex tasks, record discovery, explicit `lw-status`, and ordinary status-request selection and behavior. Preserve raw task results and usage/limit context. Discovery alone is not behavior proof.
6. Reconcile review, QA, CI, installed hashes, fresh-task evidence, immutable plugin-artifact provenance, the captured reviewed-head SHA, and private `main`. Only then may the controller create an internal tag.

## Rollback

No external release action has occurred, so there is nothing deployed to roll back in Phase A. If the local candidate must be abandoned before push, preserve this evidence and create a new corrective commit rather than rewriting shared history.

If a later authorized release fails its final gate, stop further promotion and identify the last verified private-main commit, plugin tree, cachebuster, and canonical hashes from its release record. Because the private marketplace resolves `plugins/LEAPWare-status` from the repository, reinstalling while failed candidate content remains on private `main` would reinstall that failed content. First create and review a corrective or revert commit on private `main` that restores the verified plugin tree **and its verified cachebuster**; do not use a detached checkout or rewrite history. Push that recoverable commit, verify CI for its exact commit, and synchronize local `main` with `origin/main`. Only then reinstall from `LEAPWare-status@LEAPWare-Status`, require raw installed bytes to match the restored private-main canonical Git-blob hashes, and rerun fresh-task discovery, explicit `lw-status`, and ordinary status behavior checks. Record any raw-byte mismatch even if line-ending normalization aids diagnosis. Do not claim rollback succeeded until the corrected private-main identity, installed canonical hashes, and fresh-task evidence are recorded. The old cache version `0.1.0+codex.20260909223703` is historical evaluation provenance only, not an approved rollback target unless the controller verifies it is the relevant known-good private-main artifact.

## Recovery note

LEAPWare-continuity is uninstalled and inactive. The SDD ledger and this source-controlled record preserve the objective, evidence, pending gates, authority boundary, and next action; they do not provide hook-backed continuity protection.
