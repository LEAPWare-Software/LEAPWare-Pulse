# LEAPWare-Pulse private release candidate — 2026-09-10

## Verdict

**PHASE B EXTERNAL EVIDENCE RECORDED; NOT RELEASED.** Clean branch/main CI, private-main integration, installation/hash verification, and fresh-task checks are established for the unchanged plugin tree at `acde222`. This evidence-only commit is a new reviewed release head and still requires its own final exact-head branch/main CI, final review, and release verification before any tag.

## Candidate identity

- Repository: private `LEAPWare-Software/LEAPWare-Pulse`; LEAPWare-only scope.
- Local branch: `work/status-private-release`.
- Evidence baseline: `f17832fdca1bdd5efd6690c56c0abfd9473fe347` (`Record corrected status behavior reruns`).
- Immutable plugin packaging provenance: `977c1f7d19727b4058ed900ede015d7e16a1c21d` (`Prepare private status release candidate`). This identifies the plugin artifacts only; it is not the release head to push or integrate.
- Plugin: `LEAPWare-Pulse`.
- Candidate plugin version: `0.1.0+codex.20260910194515`.
- Manifest change mechanism: `py -3.12 C:\\Users\\leapw\\.codex\\skills\\.system\\plugin-creator\\scripts\\update_plugin_cachebuster.py plugins\\LEAPWare-Pulse`.
- Marketplace/configuration changes: none. The cachebuster helper changed only `plugins/LEAPWare-Pulse/.codex-plugin/plugin.json`.
- Canonical manifest identity: Git blob `c5c626606700432d5f9b9e653576fd9253979dc5` at the candidate packaging commit; SHA-256 of those exact Git-blob bytes is `e8f873c019cc4e36fd15f2c2b1725a7b633a6d7a2b798b9c7d44abf25d941af8`.
- Canonical candidate file SHA-256 values (hash the bytes addressed by the candidate commit, not checkout bytes): `plugin.json` `e8f873c019cc4e36fd15f2c2b1725a7b633a6d7a2b798b9c7d44abf25d941af8`; `SKILL.md` `797e4ad926d0218bc63f5522934d2b317e748cdd853cabcc4932e57cf54fbb4a`; `agents/openai.yaml` `6b79f338be5760fa77e60f14760c14bad2ee99d78c89715789e8e110cc790129`.
- Windows checkout note: before the packaging commit, `plugin.json` checked out with CRLF hashed to `117b375a6c6fdeae4c4626e896d569813325a704ba6e9b6782df415d8e6f65be`. That is a checkout-byte observation only; it does **not** identify the committed candidate and must not be used for private-main or installed-artifact comparison.

The Phase A evidence-fix commits do not modify the plugin tree or version. Use the canonical candidate file hashes above for later private-main and installed-artifact comparison; record separately any platform checkout-byte hash if that diagnostic is needed. The reviewed release head is the final `work/status-private-release` branch tip that descends from `977c1f7` and contains every approved evidence fix; its exact SHA is captured at push time.

## Established evidence

| Evidence class | Established result | Source-bound limit |
| --- | --- | --- |
| Deterministic tests | 12/12 mutation/acceptance tests passed at `f17832f`; Phase A reruns are recorded below. | Tests verify source invariants, not installed behavior. |
| Repository validator | `validate_pulse_plugin.py` passed at `f17832f`; Phase A rerun is recorded below. | Repository validator is not Codex ingestion. |
| Plugin-creator validator | Passed at `f17832f`; Phase A rerun is recorded below. | Manifest validation does not install the plugin. |
| Independent behavioral QA | Corrected-source `BHV-03`, `BHV-04`, and selected `BHV-01` regression passed 38/38 properties; initial failures remain preserved. | `BHV-02` and `BHV-05`–`BHV-07` were not rerun against corrected source; evaluations were source-path tests, not live discovery. |
| Source review | Task 3 review of `9083181..f17832f` recorded no findings. | Does not cover remote/CI/runtime gates. |

## Remote CI — RED warning evidence

- GitHub Actions run: `34525323554`.
- Exact reviewed head tested: `ddf26ed`.
- Test result: 12/12 tests passed.
- Gate verdict: **RED / not accepted**. GitHub emitted a Node.js 20 deprecation annotation because `.github/workflows/validate.yml` used `actions/checkout@v4` and `actions/setup-python@v5`.
- Local remediation: this reviewed change updates only those action majors to `actions/checkout@v7` and `actions/setup-python@v7`.
- Required next evidence: push the new final reviewed tip, capture its exact remote SHA, and record a clean GitHub Actions run for that SHA. Do not treat run `34525323554` as the final CI gate.

## Clean CI and private-main integration — `acde222` plugin tree

- Exact plugin-tree head: `acde2221594b89b740744550b3a42a95925a51a4` (`Update CI actions runtime`).
- Private branch CI: run `34525852462`, job `103034436406`; success, 12-test step success, zero annotations.
- Private main integration: `main` fast-forwarded and pushed clean to the same exact `acde222` SHA.
- Private-main CI: run `34525948672`, job `103034754774`, for the same exact SHA; success, 12-test step success, zero annotations.
- Scope: these clean results supersede the v4/v5 warning as CI evidence for the unchanged `acde222` plugin tree. They do not establish CI for this later evidence-only documentation commit.

## Installed package identity — `acde222` plugin tree

- Installed/enabled package: `LEAPWare-Pulse@LEAPWare-Pulse`, version `0.1.0+codex.20260910194515`.
- Installed path: `C:\\Users\\leapw\\.codex\\plugins\\cache\\LEAPWare-Pulse\\LEAPWare-Pulse\\0.1.0+codex.20260910194515`.

| File | Canonical committed Git-blob SHA-256 | Local Windows checkout raw SHA-256 | Installed raw SHA-256 | Normalized installed SHA-256 | Result |
| --- | --- | --- | --- | --- | --- |
| `plugin.json` | `e8f873c019cc4e36fd15f2c2b1725a7b633a6d7a2b798b9c7d44abf25d941af8` | `117b375a6c6fdeae4c4626e896d569813325a704ba6e9b6782df415d8e6f65be` | `e8f873c019cc4e36fd15f2c2b1725a7b633a6d7a2b798b9c7d44abf25d941af8` | `e8f873c019cc4e36fd15f2c2b1725a7b633a6d7a2b798b9c7d44abf25d941af8` | installed raw and normalized values match canonical; checkout CRLF is separately recorded |
| `SKILL.md` | `797e4ad926d0218bc63f5522934d2b317e748cdd853cabcc4932e57cf54fbb4a` | `797e4ad926d0218bc63f5522934d2b317e748cdd853cabcc4932e57cf54fbb4a` | `797e4ad926d0218bc63f5522934d2b317e748cdd853cabcc4932e57cf54fbb4a` | `797e4ad926d0218bc63f5522934d2b317e748cdd853cabcc4932e57cf54fbb4a` | exact raw/canonical match |
| `agents/openai.yaml` | `6b79f338be5760fa77e60f14760c14bad2ee99d78c89715789e8e110cc790129` | `6b79f338be5760fa77e60f14760c14bad2ee99d78c89715789e8e110cc790129` | `3b5e96af19a8aae808f87198000eb6c44e70026d596c92484129a88480596067` | `6b79f338be5760fa77e60f14760c14bad2ee99d78c89715789e8e110cc790129` | installed CRLF-only raw difference; normalized installed equals canonical |

The installed `openai.yaml` raw-byte difference and the local checkout `plugin.json` raw-byte difference are explicitly treated as CRLF representation facts. They are not substituted silently for identity: canonical Git-blob and normalized installed results are recorded alongside the raw values.

## Fresh-task validation — `acde222` plugin tree

Environment/date: fresh Codex desktop tasks on the Windows host, 2026-09-10. All three tasks completed and their final messages below were untruncated. The explicit and ordinary tasks had recoverable failed stale/missing-path lookups before locating and reading the current installed version; those lookup errors do not alter the final message evidence.

### Discovery task `01a08cfd-b8d8-7b21-b22c-91e0f3ddae91` — exact final message

~~~text
Available: “LEAPWare-Pulse:LEAPWare-Pulse”
“Report project status as a full-plan checklist with subtasks, evidence-based completion percentages and compact progress bars.”
~~~

### Explicit `lw-pulse` task `01a08cfd-c342-7da0-8b74-4ae8dda901b0` — exact final Markdown

~~~markdown
**Objective:** Deliver Atlas export. **Status:** In progress — 75% (3/4 verified).

**Percentages = verified checklist criteria, not effort or time.**

- [x] **Discovery — Complete · 100% (2/2)** `[##########]`
  - [x] D1: Schema approved with evidence.
  - [x] D2: Field inventory approved with evidence.

- [ ] **Implementation — Active · 50% (1/2)** `[#####-----]`
  - [x] I1: Serializer complete with passing tests.
  - [ ] **Next:** I2: Production-shaped fixture run — not started.

**Next action:** Run the production-shaped fixture.
~~~

The similarly shaped value `01a08cfd-c342-7da0-b8d2-15e4b7265812` is not a thread ID and is excluded from this evidence.

### Ordinary automatic-use task `01a08cfd-cc28-7170-b328-9dccc2179ca1` — exact final Markdown

~~~markdown
**Objective:** Finish Orion release. **Status:** Incomplete.

**Percentages = verified checklist criteria, not effort or time.**

- [ ] **Release — Unverified · 66% (2/3)** `[######----]`
  - [x] **O1:** Accessibility fixes merged with evidence.
  - [x] **O2:** Staging acceptance completed with evidence.
  - [ ] **O3 — Unverified:** Production deployment; no deployment record exists.

**Next action:** Obtain production deployment evidence to verify O3.
~~~

Scope: these are live discovery, explicit invocation, and ordinary automatic-use checks for the installed `acde222` plugin tree. The ordinary result is behavioral compatibility evidence, not direct internal skill-selection telemetry. None of these tasks validates behavior of this later evidence-only documentation commit.

Full provenance, raw outputs, hashes, and limits for the independent behavioral QA are in `docs/evidence/behavioral-validation-2026-09-10.md`; that file does not contain the Phase B live fresh-task outputs recorded above. The Task 1 deterministic-validator report and Task 3 correction report are retained under `.superpowers/sdd/2026-09-10-pulse-release/`.

## Phase A local gate results

The commands in this section ran against the uncommitted local candidate after the cachebuster and documentation changes, before the candidate commit.

- `py -3.12 -m unittest discover -s tests -p 'test_*.py' -v` — exit 0; 12 tests ran in 0.345 seconds; all passed.
- `py -3.12 scripts\validate_pulse_plugin.py plugins\LEAPWare-Pulse` — exit 0; `LEAPWare-Pulse validation passed`.
- `py -3.12 C:\Users\leapw\.codex\skills\.system\plugin-creator\scripts\validate_plugin.py plugins\LEAPWare-Pulse` — exit 0; plugin validation passed.
- `git diff --check` — exit 0; no whitespace errors. Git emitted only its non-failing CRLF-to-LF working-copy warning for `plugin.json`.
- No deterministic test was skipped. The established required independent behavioral reruns were `BHV-03`, `BHV-04`, and selected `BHV-01`; their evidence reports no skip, error, or truncation within that protocol.

## Phase B evidence-commit local gates

- `py -3.12 -m unittest discover -s tests -p 'test_*.py' -v` — exit 0; 12 tests ran in 0.282 seconds; all passed.
- `py -3.12 scripts\validate_pulse_plugin.py plugins\LEAPWare-Pulse` — exit 0; `LEAPWare-Pulse validation passed`.
- `py -3.12 C:\Users\leapw\.codex\skills\.system\plugin-creator\scripts\validate_plugin.py plugins\LEAPWare-Pulse` — exit 0; plugin validation passed.
- Workflow action-major check — exit 0; requires `actions/checkout@v7`, `actions/setup-python@v7`, and the Python 3.12 test command, and rejects the earlier v4/v5 values.
- Installed normalized package hashes match the canonical values above; `git diff --exit-code acde222 -- plugins/LEAPWare-Pulse` and `git diff --check` exit 0. This evidence-only change does not alter the `acde222` plugin tree.

## Current blockers and exact planned external checks

1. Confirm the final reviewed evidence/documentation branch tip descends from `acde222` and contains all approved evidence. Push that reviewed tip without further change, capture its exact remote SHA, and confirm clean branch CI for that SHA.
2. Complete the whole-branch source/release review against that exact reviewed-head SHA and resolve every required finding before integration.
3. Integrate that same reviewed-head SHA into private `main`; verify local `main` and `origin/main` identify the same SHA.
4. Confirm clean private-main CI for that same integrated SHA.
5. Reconcile review, QA, CI, established installed hashes, fresh-task evidence, immutable plugin-artifact provenance, the captured reviewed-head SHA, and private `main`; then make the final release-readiness decision.
6. Create an internal tag only after the release-readiness decision passes.

## Rollback

No internal tag or final release verdict has occurred. The `acde222` plugin tree is already integrated into private `main` and installed under the evidence above; if the final reviewed evidence head must be abandoned, preserve this record and use a new corrective or revert commit rather than rewriting shared history.

If a later authorized release fails its final gate, stop further promotion and identify the last verified private-main commit, plugin tree, cachebuster, and canonical hashes from its release record. Because the private marketplace resolves `plugins/LEAPWare-Pulse` from the repository, reinstalling while failed candidate content remains on private `main` would reinstall that failed content. First create and review a corrective or revert commit on private `main` that restores the verified plugin tree **and its verified cachebuster**; do not use a detached checkout or rewrite history. Push that recoverable commit, verify CI for its exact commit, and synchronize local `main` with `origin/main`. Only then reinstall from `LEAPWare-Pulse@LEAPWare-Pulse`, record raw installed hashes, normalize documented CRLF representation differences for these text artifacts, and compare those normalized hashes to the restored private-main canonical Git-blob hashes. Do not treat a raw mismatch as silently equal: record the raw and normalized values and the line-ending reason. Rerun fresh-task discovery, explicit `lw-pulse`, and ordinary status behavior checks. Do not claim rollback succeeded until the corrected private-main identity, installed canonical hashes, and fresh-task evidence are recorded. The old cache version `0.1.0+codex.20260909223703` is historical evaluation provenance only, not an approved rollback target unless the controller verifies it is the relevant known-good private-main artifact.

## Recovery note

LEAPWare-continuity is uninstalled and inactive. The SDD ledger and this source-controlled record preserve the objective, evidence, pending gates, authority boundary, and next action; they do not provide hook-backed continuity protection.
