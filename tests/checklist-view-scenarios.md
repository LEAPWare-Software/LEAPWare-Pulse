# LEAPWare Pulse checklist-view behavioral scenarios

This is the concise, synthetic scenario contract suitable for repository review. Give a fresh evaluator the complete `LEAPWare-Pulse/SKILL.md`, every directly required local reference such as `references/formats.md`, and exactly one scenario below. Instruct it to render only the status report from supplied facts, use no tools, and perform no external action. Do not provide expected properties to the evaluator. Retain its raw output and judge every property manually.

Shared properties: preserve every stable required-criterion identity exactly once; count only completion established by relevant current evidence; failed, partial, blocked, stale, and unverified work contributes zero; optional and cancelled/deferred work is excluded and never marked complete; authorized scope changes are explained; unknown denominators remain Not yet measurable without invented rows/counts; end with one exact next action; status authority causes no action.

## BP-01 — explicit Panorama, stale evidence

Request `lw-pulse panorama`. M1 Build: P1 complete by AR-10; P2 earlier EV-18 pass superseded by EV-22 exit 1 schema mismatch. M2 Validate: P3 complete by AC-4; P4 blocked on staging access, owner unassigned. Four required criteria.

Expect Panorama in M1/M2 order with child criteria; each milestone `50% (1/2) [#####-----]`; P2 reopened with failure detail; P4 blocker and unassigned owner; percentage legend once; no invented plan-wide percentage.

## BP-02 — natural Panorama, unknown plan

Request `LEAPWare Pulse Panorama`. Objective approved; incomplete draft and unmapped prototype exist; no approved milestones, criteria, weighting, or denominator.

Expect plan-level Not yet measurable with no number/bar and no invented milestone/checklist row. Preserve objective and make approval of the required plan the next action.

## BP-03 — explicit Brief, added scope

Request `lw-pulse brief`. Required B1–B4 are complete across Discover, Implement, Implement, Validate. Owner decision D-12 added required B5 in Validate; no approval exists. BO-1 in Adopt is optional/unapproved and incomplete.

Expect one flat B1–B5 checklist in execution order with milestone context on every row; one `Verified criteria: 4/5` count and count legend; no percentage/bar; basis-change explanation; BO-1 separate and excluded.

## BP-04 — natural Brief, failed/unverified/active

Request `LEAPWare Pulse Brief`. Q1/Q2 complete in Prepare. Q3 Validate drill failed in DR-9 exit 1 for missing restore permission. Q4 Validate is unsupported chat claim, hence unverified. Q5 Approve has comments but no sign-off, hence active. Five required.

Expect one flat Q1–Q5 checklist with milestone context; one `Verified criteria: 2/5` count and count legend; no percentage/bar; distinct Failed, Unverified, Active states; Q3 blocker preserved.

## BP-05 — explicit Focus, all groups

Request `lw-pulse focus`. F1 Active, F2 Next, F3 Blocked on missing certificate with owner unassigned, F4 Unverified, F5 Remaining, F6 Complete, F7 Cancelled by D-9 and removed from scope. F1–F6 are required across M1/M2.

Expect state groups in the required order, all seven criteria once with parent milestone context, F3 blocker/owner, F7 unchecked; one `Verified criteria: 1/6` count and count legend; no percentage/bar.

## BP-06 — natural Focus, stale reopening/scope

Request `LEAPWare Pulse Focus`. R1 Release build complete. R2 Release validation was marked complete, then SEC-31 superseded it with a critical-dependency failure caused by a stale database; security team owns remediation/rescan. R3 is Next. R4 cancelled by D-14 and removed. RO-1 optional/unapproved. R1–R3 required.

Expect Complete, Unverified/Blocked, Next, Cancelled groups as relevant; criteria once with parent context; stale reopening and owner preserved; one `Verified criteria: 1/3` count and count legend; no percentage/bar; optional outside total.

## BP-07 — one-off explicit mode override

Recorded preference is Brief. Request one-off `lw-pulse focus`, explicitly without changing the preference. T1 Complete in Design, T2 Next in Build, T3 Blocked in Build on missing certificate owned by Elena, T4 Unverified in Validate. Four required.

Expect Focus groups and one `Verified criteria: 1/4` count with no percentage/bar. Explicit request overrides preference for this response; report makes no preference write or persistence claim.

## BP-08 — `lw-status`, partial denominator/unsupported check

Request bare `lw-status`. M1 Package has exactly U1/U2: U1 is checked in task file but has no supporting evidence; U2 complete by signed manifest. M2 Operate has known required U3 complete by drill, but owner says the full required checklist and denominator remain unknown.

Expect default Panorama. M1 `50% (1/2) [#####-----]`, U1 Unverified, U2 complete. M2 Not yet measurable with U3 once and no numeric count/bar or anonymous invented checkbox criterion.

## BP-09 — natural checklist status, stale Git evidence

Request generic `checklist status`. Integration has G1 feature branch clean at F-20 with relevant tests complete; G2 task checked but actual main worktree last observed at M-19; G3 local `origin/main` says F-20 but was fetched before push and no current remote proof exists. Three required.

Expect default Panorama `33% (1/3) [###-------]`; only G1 checked; G2/G3 Unverified; clean branch and task check do not prove main integration; stale tracking ref does not prove current remote; next steps follow main then remote evidence order.

## BP-10 — bare `lw-pulse` default

Request bare `lw-pulse`. Ship: Z1 complete by artifact receipt; Z2 installation not started/no record. Two required.

Expect default Panorama with both child criteria and `50% (1/2) [#####-----]`; Z2 unchecked; project not complete.

## BP-11 — 99/100 boundary

Request `LEAPWare Pulse Panorama`. One Migration verification milestone has V001–V100; signed ledger verifies V001–V099; V100 cold-backup restore is pending with no execution record.

Expect `99% (99/100) [#########-]`, not 100/full; all IDs inspectable; V100 unchecked with evidence gap; no completion/readiness claim.
